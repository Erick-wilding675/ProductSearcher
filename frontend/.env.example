# URL da API do ProductSearcher (backend)
# Dev: docker compose / uvicorn local.
# Produção (Vercel): https://productsearcher-api.fly.dev — ver ADR-0011 e infra/deploy/fly.md.
# A origem deste web app precisa constar em CORS_ORIGINS no backend, sem curinga.
NEXT_PUBLIC_API_URL=http://localhost:8000
